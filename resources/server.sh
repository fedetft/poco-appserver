#!/bin/sh

# Use python to start a web server and deliver the following static resources
# to the web application. For a production environment, consider using a
# real web server such as nginx
python -m SimpleHTTPServer 9990
